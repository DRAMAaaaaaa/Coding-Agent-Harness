# 课程反思

## Superpowers 技能

这个项目让我最直接地看到，技能的价值并不只是生成更多内容，而是改变做事顺序。`brainstorming` 先迫使我把“做一个完整 Coding Agent”拆成可回答的问题，再将结果固化到 `SPEC.md`；`writing-plans` 又把抽象要求变成准确文件、依赖、预期失败测试和验证命令。随后通过隔离 worktree、TDD 和双重审查，实施不再依赖模型临场发挥。这个顺序增加了前期成本，却减少了后期对“需求到底是什么”的争论。仓库也记录了冷启动审计多次阻止不完整计划进入实现，因此我理解到，流程的目的不是让 Agent 显得繁忙，而是让关键决定在写代码前变得可检查、可拒绝和可追溯。

## TDD

我选择 Task 5 的交付文档契约作为 RED→GREEN 的例子。独立审查指出功能文档写了错误路径，也没有准确定位 OpenAI-compatible Provider 运行时。新增契约后，测试首先因为缺少 `src/coding_agent_harness/providers/openai_compatible.py` 这一证据而失败。这个 RED 不是语法错误，而是证明“文档声称的能力无法从仓库定位”。补齐真实路径、Mock 主路径和演示顺序后，定向测试转绿；第二轮审查又发现 fixture 指令顺序不清，于是继续收紧契约。最终记录为 Python `849 passed, 15 skipped`、Vitest `66 passed`、Playwright `4 passed, 1 skipped`，但这些结果只支持对应代码和本地流程，不能外推为公网或真实 Provider 已通过。这个过程让我认识到，文档同样可以用测试约束真实性。

## subagent-driven

subagent-driven 的主要收益是把实现、规约符合性审查和质量审查分开。冷启动智能体只读取规定的 `SPEC.md` 和 `PLAN.md`，能暴露主控因熟悉上下文而忽略的歧义；Task 5 初审给出的 `0/4/0` 也说明“实现者自认为完成”不能替代独立检查。不过，智能体报告本身仍不是最终证据。主控必须核对 Git 范围、文件差异、测试命令和退出状态，并确认审查意见没有把环境失败误判成产品缺陷。多智能体还会增加上下文传递和过程记录成本，若任务边界不清，反而可能重复劳动。因此我认为它适合职责可切分、验收标准明确的任务，不应被当成自动保证质量的捷径。

## SPEC / PLAN

`SPEC.md` 定义稳定的产品边界，`PLAN.md` 负责把边界转换为可执行步骤，两者并不保证一开始就完全正确。项目中出现过两个“Task 5”：早期路线的 Task 5 是项目识别、扫描与 worktree；2026-08-14 联合计划的 Task 5 则是最终工作台交付文档与学生反思提纲。若只搜索编号，很容易把历史结论写入当前状态。最终通过根文档描述当前产品、`docs/archive/README.md` 统一导航历史规格和计划，并在 `AGENT_LOG.md` 保留时间线，解决同名问题。我的体会是，计划可以修订，但不能悄悄改写历史；当前结论、历史证据和延期事项必须分层存放，读者才能判断某条结论在什么时间和范围内有效。

## Prompt / Context

长项目中，Prompt 和 Context 的关键不是塞入所有信息，而是给出足够且可核对的边界。冷启动审计采用受限上下文，能够检验 `SPEC.md` 和 `PLAN.md` 是否真的支持陌生执行者开始工作；实现阶段则需要准确的文件、接口和失败预期。上下文过少会遗漏依赖，过多又会让历史状态覆盖当前事实。项目文档因此尽量使用具体路径和命令，例如 `docs/FEATURES.md` 将能力连接到源码与测试，`docs/DEPLOYMENT.md` 明确区分静态配置和动态部署。我也必须克制结论：WebUI 有 Provider 配置不等于已完成连接测试，Compose 可解析不等于 Docker daemon 已运行，公网说明存在更不等于 Nginx、ECS 浏览器或真实外网 Provider 已验收。

## 凭据与分发

安全要求贯穿设计、测试和交付，而不是最后补一段免责声明。课程主路径使用 `ScriptedMockProvider`，不访问网络，也不需要真实 Key；`.env.example` 只能出现占位值，凭据不能进入日志、异常、快照、事件或 `AGENT_LOG.md`。`scripts/secret_scan.py` 还暴露过文件名片段导致的误报，后续修复既要消除误报，也不能放宽真实 token 的检测。这让我理解到安全扫描本身也需要精确契约。分发方面，镜像采用非 root、只读项目挂载和独立 `/state`，Compose 默认只绑定 localhost；这些是已测试的配置约束，不是多租户安全保证。常驻公网 Mock 仍缺少 HTTPS 和认证，只能处理无真实数据、无真实 Key 的演示项目。

## 方法论批判

这套流程的优点是可追溯，但成本同样明显。长规格、长计划和大量过程日志会提高阅读负担；多轮冷启动审计可能重复发现相似问题；为每个行为保留 RED、GREEN、审查和提交证据，也会让小改动显得笨重。确定性 Mock 能稳定证明 Agent 循环、治理、反馈和停止逻辑，却不能代表真实模型的延迟、格式漂移、限流和网络故障。人工批准降低高风险副作用，却可能形成“用户只是机械点击”的新风险。隔离 worktree 保护主工作区，但同一 UID 的恶意原生进程仍可能篡改私有状态，`DW-05-001` 已明确把独立 OS 身份、ACL 或 broker 延期。因此，过程证据应服务于风险，而不是追求文档数量；对低风险、可逆的小改动，可以缩短模板，但安全边界和验收真实性不能缩减。

## 如果重做

如果重新开始，我会保留先规格、再计划、再 TDD 和独立审查的主线，但会更早建立“当前状态索引”和统一术语表，避免历史 Task 编号、分支状态与产品能力混在一起。我也会把动态验收矩阵提前放进计划，将“代码契约已测”“本地进程已跑”“Docker daemon 已跑”“公网浏览器已跑”“真实 Provider 已跑”分成不同列，禁止相互替代。对于子智能体，只提供单 Task 的最小上下文和明确报告格式，减少重复阅读。改进不能只写成愿望：例如真实 Provider 验收应先定义脱敏、超时、限流和失败注入测试；公网部署应增加 HTTPS、认证和浏览器 E2E；`DW-05-001` 则应以跨 Windows/Linux 的权限安装、恢复和对抗测试作为完成门禁。


